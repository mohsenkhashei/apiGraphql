{
  "name": "api",
  "version": "1.0.0",
  "description": "api with graphql and multiple database",
  "main": "app.js",
  "scripts": {
    "start": "set NODE_ENV=dev && nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1"
  }
}
